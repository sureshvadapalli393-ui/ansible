- name: variables
  hosts: local
  connection: local
  vars:
    course: Ansible
    Duration: 10hrs
    Trainer: shiva
  tasks:
  - name: print thee course info
    ansible.builtin.debug:
      msg: "course name is: {{ course }}, duration: {{ Duration }}, tainer is {{ Trainer }}"